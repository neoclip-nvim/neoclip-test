name: test autoinstall with brew

on:
  push:
    branches:
      - master
  pull_request:
  repository_dispatch:
    types:
      - test

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        neovim-version: [stable] #, nightly] # TODO run fortnightly on nightly
        plugin-manager: [lazy] #, neovim]
    runs-on: ${{ matrix.os }}
    steps:

      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@master
      - name: Cache Homebrew Bundler RubyGems
        uses: actions/cache@v4
        with:
          path: ${{ steps.set-up-homebrew.outputs.gems-path }}
          key: ${{ matrix.os }}-rubygems-${{ steps.set-up-homebrew.outputs.gems-hash }}
          restore-keys: ${{ matrix.os }}-rubygems-

      - name: Install Neovim
        uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: ${{ matrix.neovim-version }}

      # prepare to use actions from this repo
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install node
        uses: actions/setup-node@v4
      - run: npm ci

      - name: Install neoclip
        id: install-neoclip
        uses: ./actions/install-neoclip-nolib
        with:
          plugin-manager: ${{ matrix.plugin-manager }}

      - name: Check health
        uses: ./actions/checkhealth-neoclip
        with:
          nvim: ${{ steps.install-neoclip.outputs.nvim }}
